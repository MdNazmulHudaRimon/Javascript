//let's know the uses of const and let
//when a variable name is fixed and we know that it won't updates on future than we can use const instead of var
//when we know a variable can be changed in the near future we will use let instead of var

const country = "Bangladesh";
console.log(country);
let num = 15;
num = 16;
console.log(num);